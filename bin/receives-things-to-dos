#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../thaings'

begin
  raw = $stdin.read.force_encoding('UTF-8').strip
  ReceivesThingsToDo.new(ThingsInput.new(raw)).call
rescue ThingsInput::InvalidInput => e
  $stderr.puts e.message
  exit 1
end
