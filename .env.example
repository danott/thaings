# Things URL scheme auth token
# Get from: Things → Settings → General → Enable Things URLs → Manage
THAINGS_THINGS_AUTH_TOKEN=your_token_here

# Path to system prompt file for to-do processing Claude sessions
# Default: default-system-prompt.md in the thaings root directory
# THAINGS_SYSTEM_PROMPT_FILE=/path/to/your/system-prompt.md

# Maximum agentic turns per to-do (default: 10)
# THAINGS_MAX_TURNS=10

# Timeout in seconds for Claude to respond (default: 300)
# THAINGS_TIMEOUT_SECONDS=300

# Comma-separated list of tools Claude can use (default: WebSearch,WebFetch)
# THAINGS_ALLOWED_TOOLS=WebSearch,WebFetch

# Path to claude binary (default: /opt/homebrew/bin/claude)
# THAINGS_CLAUDE_PATH=/opt/homebrew/bin/claude
